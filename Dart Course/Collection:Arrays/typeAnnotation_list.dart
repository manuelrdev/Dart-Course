void main() {
  // This is to explicitely declare the type of the variable of this list
  List<String> cities = ['London', 'Paris', 'Moscow'];

  // In this version, we are telling dart that, this list if composed only by Strings.
  var brands = <String>['Ford', 'Subaru', 'Porsche'];
}
